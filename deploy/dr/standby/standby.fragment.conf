primary_conninfo = 'host=@PRIMARY_HOST@ port=5432 user=edutrack_replication sslmode=verify-full sslrootcert=/etc/edutrack-dr/primary-ca.crt passfile=/run/credentials/edutrack-replication.pass'
restore_command = 'pgbackrest --stanza=edutrack archive-get %f %p'
hot_standby = on
