# Type Path Mode User Group Age
d /run/edutrack-config-agent 0750 edutrack-config-api edutrack-config-api -
# The agent creates /run/edutrack-config-agent/agent.sock atomically with mode 0660 and group
# edutrack-config-api; tmpfiles must not pre-create a regular file at the socket path.
